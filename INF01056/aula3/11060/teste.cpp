#include <algorithm>
#include <cstdio>
#include <map>
#include <vector>
#include <queue>
#include <iostream>
using namespace std;


int main() {

    string b1;
    vector< vector<string> > a;

    while(cin >> b1) {
            cout << b1;
            a.push_back([]);
            a[0].push_back(b1);
        }
    return 0;
}
